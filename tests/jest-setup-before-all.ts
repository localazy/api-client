/* eslint-disable-next-line */
import 'tsconfig-paths/register';
/* eslint-disable-next-line */
import 'dotenv/config';

export default async (): Promise<void> => {
  // This is run once before all tests.
  // Note: `global` doesn't work from here.
};
